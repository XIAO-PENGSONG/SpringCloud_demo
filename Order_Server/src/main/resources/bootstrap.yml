server:
  port: 8100

spring:
  application:
    name: cloud-order
#配置nacos服务器的地址
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
#      config:
#        file-extension: yaml
#监控配置
management:
  endpoints:
    web:
      exposure:
        include: '*'
